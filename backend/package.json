{
  "name": "medstack-backend",
  "version": "1.0.0",
  "description": "MedStack backend — medication safety: Gemini Vision label reading + Firecrawl live interaction data + plain-English severity",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
