# Copy to .env and fill in. NEVER commit the real .env.
# On Cloud Run, set these as environment variables / secrets instead.
GROQ_API_KEY=your_groq_key_here
FIRECRAWL_API_KEY=your_rotated_firecrawl_key_here
PORT=8080
